.batch-wrapper
  .batch-header
    %h1.batch-title
      Batch Details

  .batch-edit-box
    = form_for(@batch, url: admin_batch_path(@batch)) do |f|
      .batch-field
        = f.label :name
        = f.text_field :name, class: 'batch-input'
      .batch-button-container
        = link_to 'Cancel', admin_rebate_forms_path, class: 'batch-cancel-button'
        = f.submit 'Save', class: 'batch-submit-button'

