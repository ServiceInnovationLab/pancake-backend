#nav.pure-u
  %a.nav-menu-button{href: "#"} Menu
  .nav-inner
    / %button.primary-button.pure-button Compose
    .pure-menu
      %ul.pure-menu-list
        - if current_user.present?
          %li.pure-menu-heading Council
          %li.pure-menu-item
            %a.pure-menu-link{href: admin_rebate_forms_path}
              Forms
              %span.email-count (#{RebateForm.all.size})
          %li.pure-menu-item
            = link_to new_user_invitation_path, class: 'pure-menu-link' do
              Invite new user

        - else
          %li.pure-menu-item
            = link_to new_user_session_path, class: 'pure-menu-link' do
              Sign in


        - if current_user.present?
          %li.pure-menu-item
            = link_to destroy_user_session_path, method: :delete, class: 'pure-menu-link' do
              Sign Out
        %li.pure-menu-heading Public info
        / %li.pure-menu-item
        /   %a.pure-menu-link{href: "#"}
        /     %span.email-label-personal>
        /     Personal
        %li.pure-menu-item
          %a.pure-menu-link{href: "/app/"}
            %span.email-label-work>
            Apply now
        %li.pure-menu-item
          %a.pure-menu-link{href: "/api/docs"}
            %span.email-label-travel>
            API Docs
